# Copy to .env.local and fill in after deploying worker/ (see worker/README.md)

# VAPID public key printed by `npx @pushforge/builder vapid` inside worker/
VITE_VAPID_PUBLIC_KEY=

# URL of the deployed Cloudflare Worker (e.g. https://holi-push.<your-subdomain>.workers.dev)
VITE_PUSH_WORKER_URL=
