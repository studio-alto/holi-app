{
  "name": "holi-push-worker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "vapid": "npx @pushforge/builder generate-vapid-keys"
  },
  "dependencies": {
    "@pushforge/builder": "^1.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
